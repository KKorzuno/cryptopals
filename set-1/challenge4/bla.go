


calculate sums of multiples for a set()
	add multiplication of last element of the set by all the other elements
	calculate multiples for a set minus the one that we used()
}


func calcSumsOfMultiples(slice, currentsum)
	for i,e := range slice {
		for ii,ee := range slice[1:max]{
		{
			currentSum += slice[i] * slice[ii]
		}
	}
	if len (slice[1:max]) = 1 stop
	calculateSumOfMultiples(slice[one,max], currentSum)

}

{ zero, one, two, three } 
slice [one: max] = { one, two, three }
